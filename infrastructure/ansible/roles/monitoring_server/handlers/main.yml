---
- name: restart monitoring services
  ansible.builtin.systemd:
    name: "{{ item }}"
    state: restarted
    daemon_reload: yes
  loop:
    - prometheus
    - loki
    - alertmanager

- name: restart grafana
  ansible.builtin.service:
    name: grafana-server
    state: restarted
