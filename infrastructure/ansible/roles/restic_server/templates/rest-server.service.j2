[Unit]
Description=Restic REST Server
After=network.target

[Service]
Type=simple
User=backup
Group=backup
ExecStart=/usr/local/bin/rest-server --path /backup/restic --append-only --tls=false --listen :8000
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target